# パーソナルコンテンツフィルタリングシステム 要件定義

作成日：2026年8月  
ステータス：要件定義完了 → 設計・実装フェーズへ

---

## 1. プロジェクト概要

メルマガ・RSSフィード・ウェブ記事をベクトル化し、オーナーの嗜好ベクトルとのコサイン類似度でスコアリングするパーソナルコンテンツフィルタリングシステム。

- **ウェブアプリケーション**。スマートフォンがメインだがPC・タブレットでも動くレスポンシブデザイン
- セルフホスト・個人利用

### 2つの主要ユースケース

1. **享受モード**：好みのコンテンツを読み、記録し、外部に紹介する
2. **トレーニングモード**：スコアが際どいコンテンツへの評価を下し、嗜好ベクトルの精度を向上させる

---

## 2. オーナー情報・環境

### ハードウェア・インフラ

- 自宅にZimaBoard 832（Intel Celeron N3450・8GB RAM）を運用中
- CasaOS上でDockerコンテナを複数稼働（AdGuard、dnsmasqなど）
- Tailscaleで外部からアクセスできる構成

### 開発スタック・経験

**現行スタック経験：** TypeScript、Vue.js、Bun

**過去経験：** Emacs Lisp、Common Lisp、PHP、JavaScript、AngularJS1、Java（少し）、Objective-C

**学習中：** Golang、Rust、Elixir

**プログラミング哲学：**
- Lisp系・関数型プログラミング・構造化プログラミングを主軸とする
- スコープを狭く保ち、意味に名前をつけることを重視
- Elixirのパイプ、Lispのリスト処理、キーワードによる真一致の比較を好む

### UIリファレンス

既存の `feed-reader` リポジトリ（GitHub: `peccu/feed-reader`）にtmblr text風のRSSリーダーがNext.jsで実装されている（2年前）。UIスタイルはこれを参考にしつつ、技術スタックは本プロジェクトで新規構築する。

---

## 3. ユースケース詳細

### コンテンツソース

| ソース | 取り込み方法 | 備考 |
|---|---|---|
| RSSフィード | 定期ポーリング | Hacker News・Lobstersなど |
| ウェブ記事 | URL手動投入 | APIにURLをPOSTして取り込む |
| HTML汎用投入 | 本文HTMLをREST APIにPOST | メルマガ・任意ページ対応 |
| Gmailポーリング | 送信元ホワイトリストでフィルタ | **フェーズ2以降** |
| SNS（Blueskyなど） | AT Protocol | **フェーズ2以降** |

### 読み方のフロー

1. Ingesterがバックグラウンドでコンテンツを取得・スコアリングして未読キューに積む
2. ユーザーはキューをスコア順に消化していく
3. アプリ内リーダーモードで本文を読む（元サイトへの遷移ではなく本文をアプリ内表示）
4. 記事に対してアクションを記録する
5. フィードバックをもとに嗜好ベクトルを更新する

### 記事に対するアクション

| アクション | 意味 |
|---|---|
| 好き / 嫌い | 嗜好ベクトル更新のための明示的フィードバック |
| あとで読む | キューに残す |
| カテゴリ付け | ガジェット・ライフハック・AI動向・日本酒など任意グループへの分類 |
| メモ・引用 | 将来何に使えそうかをテキストで記録 |
| SNSシェア | 読了記録＋外部公開（アイデンティティ表明） |
| この路線でもっと | 類似コンテンツの発見・掘り下げ検索 |
| Claudeで対話 | 記事をきっかけに思考整理→ノートとして保存・ベクトル化 |

### カテゴリ管理

- **自動クラスタリング**をベースとし、手動でのラベル付け・修正も可能
- カテゴリ例：ガジェット・サイバーデッキ、ライフハック、プログラミング、プロジェクト管理、AI動向、日本酒・クラフトビール・コーヒー、レストラン など

### 嗜好ベクトルの初期生成

ブラウザのブックマークHTMLをエクスポートして記事をスクレイピング→エンベディング→平均ベクトルで初期プロファイルを作成する。

---

## 4. 技術スタック

| レイヤー | 選択 | 決定理由 |
|---|---|---|
| エンベディング | **Jina AI Free Tier**（`jina-embeddings-v3`） | 月100万トークン無料。ZimaBoardのRAMを消費しない |
| ベクトルDB | **sqlite-vec**（SQLite拡張） | 年間〜15,000件規模。Bun.sqliteでネイティブアクセス可。単一ファイルでバックアップ容易 |
| グラフDB | **Kuzu**（埋め込み型グラフDB） | 記事・ノート間の関係表現。SQLiteと同様の埋め込み型でサーバー不要。Neo4jより軽量 |
| バックエンドAPI | **Hono**（on Bun） | TypeScript親和性が高く、Bun上で動作。ElixirのパイプやLispの関数合成に近い書き方ができる |
| スコアリング | **コサイン類似度**（Bun実装） | 外部依存なし |
| フロントエンド | **Vue.js + Vite + TailwindCSS** | Vue.js経験あり。SSR不要なのでNuxt不使用。Shadcn/uiは不使用、AIによるカスタムUIを構築 |
| Claude連携 | **claude -p**（CLIトークン方式） | APIキーではなくCLI認証トークンを使用。ZimaBoardコンテナから呼び出す |
| ホスティング | **ZimaBoard + Tailscale** | セルフホスト。外部からTailscale経由でアクセス |

### 見送った選択肢

- **Ollama（ローカルエンベディング）**：N3450はCPU-onlyでRAM競合リスクあり
- **pgvector（PostgreSQL）**：今回の規模には過剰
- **Qdrant・Weaviate**：年間数万件規模にはオーバースペック
- **Neo4j**：リソース使用量が大きくZimaBoardには不適
- **Nuxt.js**：SSR不要のためVite単体で十分
- **Shadcn/ui**：AIによるカスタムUI構築が可能なため不要

---

## 5. アーキテクチャ

### コンテナ構成（3コンテナ）

```
ZimaBoard / Docker Compose

┌─────────────────┐      ┌──────────────────────────┐
│   ingester      │      │    app                   │
│   (Bun)         │      │    (Hono / Bun)          │
│                 │      │                          │
│ - RSSポーリング  │      │ - REST API               │
│ - URL取込み     │      │ - Vue.js静的ファイル配信  │
│ - HTML POST受取 │ ←──→ │ - スコアリング           │
│ - 本文スクレイプ │  DB  │ - フィードバック受取     │
│ - Jina API呼出し│      │ - 嗜好ベクトル更新       │
│ - ベクトル保存   │      │ - カテゴリ管理           │
└─────────────────┘      └──────────────────────────┘
        │                          │
        └────────────┬─────────────┘
                     ↓
        ┌─────────────────────┐
        │   claude-worker     │
        │   (claude -p)       │
        │                     │
        │ - 記事要約・分析     │
        │ - ユーザー対話      │
        │ - ノート生成        │
        └─────────────────────┘
                     ↕
┌──────────────────────────────────────────────────┐
│  共有Dockerボリューム                              │
│  - sqlite-vec（articles / embeddings / queue /   │
│                preference_profile / notes /      │
│                actions / categories）            │
│  - Kuzu（記事・ノート・カテゴリ間の関係グラフ）    │
└──────────────────────────────────────────────────┘

↑ Tailscale経由でどこからでもアクセス可
↓ 外向き通信
┌──────────────────────────────────────────────────┐
│  Jina AI API（外部SaaS）                          │
└──────────────────────────────────────────────────┘
```

### コンテナ分離の理由

| | ingester | app | claude-worker |
|---|---|---|---|
| 起動タイミング | 定期実行・イベント駆動 | 常時待機 | 呼び出し時 |
| 処理の性質 | バッチ・非同期・重い | リクエスト/レスポンス | 対話・生成 |
| 外部通信 | Jina・RSS・スクレイピング | なし（DBのみ） | Claude CLI |
| 障害時の影響 | 次サイクルで回復すればよい | 即座にUI影響が出る | 対話機能のみ停止 |

### sqlite-vec / Kuzuの共有方法

- ingester・app・claude-workerが同一Dockerボリュームをマウント
- SQLiteはWAL（Write-Ahead Logging）モードを有効化して同時アクセス競合を防ぐ

---

## 6. データモデル（概要）

### DDDドメイン区分

| ドメイン | 責務 |
|---|---|
| **コンテンツ** | 記事・フィード・エンベディング |
| **リーダー** | 未読キュー・嗜好ベクトル・フィードバック |
| **ナレッジ** | ノート・カテゴリ・グラフ関係 |

### 主なテーブル（sqlite-vec）

- `articles`：記事本文・メタデータ
- `embeddings`：ベクトルデータ（sqlite-vec）
- `queue`：未読キュー（unread / read / skipped など）
- `preference_profile`：嗜好ベクトル（複数ベクトル対応を検討）
- `categories`：カテゴリ・タグ
- `notes`：Claude対話・メモ・引用（ベクトルも保持）
- `actions`：アクション履歴（好き/嫌い・シェア・カテゴリ付けなど）

### グラフ（Kuzu）

記事・ノート・カテゴリ間の関係を表現する。例：「この記事からこのノートが生成された」「この記事とこの記事は同じカテゴリに属する」など。

---

## 7. 嗜好ベクトル更新アルゴリズム（方針）

- 明示的フィードバック（好き/嫌い）をもとに加重移動平均で更新
- 例：`profile = profile * 0.95 + articleVec * 0.05`
- 係数は調整可能なパラメータとして管理
- 将来的に複数の嗜好ベクトル（「共有したいもの」「学びたいもの」など目的別）への拡張を検討

---

## 8. 開発方針

### 進め方

- フェーズ分けなし。インクリメンタルに実装し、順番に使えるようにしていく
- **TDD**（テスト駆動開発）：テストを先に書いてから実装する
- **DDD**（ドメイン駆動設計）：ドメインモデルを軸に設計する

### テスト・CI

- **Playwright**：E2EテストおよびVRT（ビジュアルリグレッションテスト）
- **GitHub Actions**：PRごとに各画面のスクリーンショットを確認できるCI

### リポジトリ構成（Monorepo）

GitHub: `peccu/feed-reader`（既存リポジトリを大幅改修）

Bun workspacesを使用したmonorepo構成：

```
feed-reader/
├── apps/
│   ├── app/            # Hono + Vue.js (Bun)
│   ├── ingester/       # RSS・URL・HTML取込 (Bun)
│   └── claude-worker/  # claude -p ラッパー (Bun)
├── packages/
│   ├── domain/         # DDDドメインモデル（共有）
│   ├── db/             # SQLite + Kuzu アクセス層（共有）
│   └── types/          # 共有TypeScript型定義
├── docker-compose.yml
├── .github/
│   └── workflows/      # GitHub Actions（VRT・テスト）
└── package.json        # ワークスペースルート
```

---

## 9. 未決事項

### 優先度高（実装に直結）

1. **未読キューの状態遷移**：unread / skipped / read / archived などの具体的な状態定義
2. **フィードバックの種類と嗜好ベクトルへの反映方法**：好き/嫌いの重み付け係数など
3. **本文スクレイピングの実装**：ライブラリ選定（`@extractus/article-extractor`など）
4. **ingesterのスケジューリング**：RSSポーリング間隔・cronの管理方法

### 優先度中

5. **エンベディング対象テキストの設計**：タイトルのみ・タイトル+冒頭・全文（Jinaの月1Mトークン枠との兼ね合い）
6. **嗜好ベクトルの初期生成フロー**：ブックマークHTMLインポートのUX
7. **Claude対話のセッション管理**：`claude -p`の呼び出し方・会話履歴の保持方法
8. **「この路線でもっと」の実装**：既存ベクトルでの類似検索かJina検索か

### 優先度低（後回し）

9. **Gmailポーリング**：送信元ホワイトリストで特定メールマガジンを自動取込
10. **SNS対応**：Bluesky（AT Protocol）
11. **IMAP対応**
12. **複数嗜好ベクトル**：目的別（享受用・共有用・学習用）への拡張
13. **グラフDBの活用範囲**：Kuzuで何をどう表現するかの詳細設計
