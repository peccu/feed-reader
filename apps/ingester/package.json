{
  "name": "@feed-reader/ingester",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@feed-reader/domain": "workspace:*",
    "@feed-reader/db": "workspace:*",
    "@feed-reader/types": "workspace:*",
    "@postlight/parser": "^2.2.3"
  }
}
